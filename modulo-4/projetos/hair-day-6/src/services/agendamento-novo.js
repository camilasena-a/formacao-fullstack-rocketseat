import { apiConfig } from "./api-config";

export async function novoAgendamento({id, nome, data}) {
  try {
    await fetch()
  } catch (error) {
    console.log(error)
    alert("Não foi possível agendar. Tente novamente mais tarde")
  }

}